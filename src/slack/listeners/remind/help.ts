export const HELP_TEXT = [
  "*`/bee-remind`* — scheduled reminders for this channel. Every change asks you to confirm first.",
  "",
  "*Reminders*",
  "• `list` — every reminder here, each with *Edit*, *Run now* and *Remove*, and *+ New reminder* at the bottom",
  "• `show <code>` — the message, the next fire and the rotation order",
  "• ⋮ **More actions** → **Make this a reminder** turns a message you already wrote into one",
  "• *Run now* posts immediately, still respecting holidays and cadence",
  "",
  "*Host rotation* — the bot appends `🎙 Host: @someone`, a different person each time",
  "• Set or clear the roster on the *Edit* form — leave it empty for no rotation",
  "• `show <code>` has *Skip host* and a picker to put someone up next",
  "",
  "*Holidays* — shared across every channel",
  "• `holiday add <YYYY-MM-DD>` · `holiday list` · `holiday remove <YYYY-MM-DD>`",
  "",
  "*Notes*",
  "• Times are 24-hour, Asia/Jakarta.",
  "• The rotation is shuffled, and nobody repeats until everyone has hosted. `show` has the order.",
  "• *Skip host* moves them to the back of this lap — they still get their turn.",
].join("\n");
